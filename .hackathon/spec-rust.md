# RUST layer (Codex). Read the pinned contract above first.

Files you own: `src-tauri/src/copilot_session.rs`, `commands.rs`, `lib.rs`, `types.rs`, `storage.rs`, `workspace_runs.rs`, `config.rs`, and their tests. Do not touch `src/` or `python-service/`.

1. **Detector.** In `copilot_session.rs` add `COMMITMENT_PATTERNS`, the owner and deadline regexes (compile once with `once_cell`/`std::sync::OnceLock` as the file already does for other regexes), `fn detect_commitment(text: &str, source: &str) -> Option<(String, Option<String>, Option<String>)>` (text, owner, deadline). Call it from `on_caption` for the completed turn text, after the question path, for both sources. Push a `Commitment` with `state: "caught"` and emit `copilot-commitment`. Dedup on normalized text within 60 s. Never feed copilot SAY text into it (captions only, as the question detector).
   Fixtures (test `commitment_detector_fixtures`): positives "I'll send the numbers to Wael by Monday" (owner Wael, deadline "by Monday"), "Let's get the deck to Naema before Friday" (owner Naema, deadline "before Friday"), "We need to check the SIDRA thresholds" (research, no owner, no deadline), "Action item: draft the procurement policy summary", "I can put together a comparison of the two rerankers by end of week" (deadline "by end of week"); negatives "Can you send me the numbers?", "What did we decide about the deck?", "I think we should be careful here", "Let me think about that", "I'll be honest, it did not work".
2. **Commands.** `commitment_approve` and `commitment_dismiss` per the contract; `CommitmentResult` in `types.rs` (serde, `ts` if the project generates TS from Rust types, otherwise plain). Register both in `lib.rs`. Workspace rule and task details per the contract. Queue the run through the existing path in `workspace_runs.rs` (find the function the Workspaces tab uses when a queued task is picked up; if the runner is a background loop that picks up `queued` tasks by itself, creating the task with status `queued` is enough and `run_queued` means "the loop is not paused"). Respect `agents_paused`.
   Tests: `commitment_approve_creates_task_and_queues` (with an in-memory DB, a session with one caught commitment; after approve the task exists with details starting `Caught live`, capability `write` or `research` by verb, state `approved`, event payload carries `task_id`), `commitment_approve_when_paused_queues_only`, `commitment_dismiss_marks_dismissed`, `workspace_rule_folder_name_then_live_meetings`.
3. **Meeting id.** If the session knows the current meeting id, pass it as `source_meeting_id`; otherwise `None`. Do not add columns.
4. Gate: `cd src-tauri && ADVERSARIA_DATA_DIR=$(mktemp -d) cargo test && cargo fmt --check && cargo clippy`. Reply with changed files, counts, and contract deviations.
