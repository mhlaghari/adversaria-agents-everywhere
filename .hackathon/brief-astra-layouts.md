# Astra, design pass (25 minutes): new layouts for the Copilot companion and Workspaces, checked against this code

Read-only. No file changes. Output Markdown, at most 2,500 words, no em dashes. Hackathon, hard stop at 15:30 today; a frontend worker starts from your document in 30 minutes and has 90 minutes. Settle every decision; give the worker none.

Repo: this directory. Read `HACKATHON.md`, `.hackathon/demo-script.md`, then the code:
`src/components/RecordingCompanion.tsx` (the companion; `variant` prop from `App.tsx:163/618` config `recording_view`, default "balanced"; note `setRecordingView` is never driven by window size), `src/components/CopilotCards.tsx`, `src/components/CommitmentCard.tsx` (rewritten today: type select, in-card run progress, Open draft), `src/prototype.css` (grep `companion-`, `balanced-wide`, `companion-body--copilot-focus`, `commitment-`, the `@media` blocks around lines 4880 and 5389), `src/components/workspaces/WorkspaceDetailView.tsx` (task rows, `caughtLiveAt`, Needs you / Running / Queued / Done), `src/components/workspaces/ArtifactPreview.tsx`, `src/components/WorkspacesView.tsx`, `src-tauri/tauri.conf.json` (window 1024x720), `src-tauri/src/addons.rs` and `src-tauri/src/workspace_runs.rs` (what the `visualize` capability runs and what file it writes on the Local engine).

## The problem

The companion works at full width. When the founder shrinks the window to sit beside a video call (roughly 380 to 520 px wide, full height), it is a mess: the balanced two-column layout collapses into a stacked column, the transcript and the copilot panel fight for height, the Notes / Last time / Copilot tabs plus the readiness line plus the consent bar eat the top, and CAUGHT commitment cards and ANSWERS cards scroll away together. Nothing is responsive; the only narrow variant ("transcript") is a config switch nobody flips.

## What I need from you

1. **Companion narrow layout, one recommendation plus one fallback.** For widths under the `balanced-wide` breakpoint: what is visible without scrolling, what collapses, where CAUGHT cards (need a tap) go relative to ANSWERS (read-only), where the notes input goes, whether tabs survive or become a segmented control or a sheet. Give the JSX structure (component tree with class names), the CSS as a `@media` block with real numbers, and how `layout` should be derived (window width via `matchMedia` or `ResizeObserver` on the companion root; say which and why; keep the config variant working). The transcript must stay readable while an answer streams. A commitment needing a tap must be the most prominent thing on screen the moment it lands.
2. **Companion wide layout, small corrections only.** What to tighten at 1024x720 given today's grouping (CAUGHT / ANSWERS headings, amber edge on untapped cards). Keep it to a list of at most six CSS or JSX changes with file and line anchors.
3. **Workspaces detail view for the demo.** The demo opens Workspaces after Stop to show two Live tasks: one Research, one Visualize (a solutions architecture diagram). Decide how a finished Visualize artifact is shown in-app: what `ArtifactPreview.tsx` handles today (HTML? SVG? PNG? drawio XML?), what the Local engine actually writes for `visualize` (read `addons.rs` / `workspace_runs.rs` and say exactly), and the smallest change so the diagram renders inline in the task row when it is awaiting review. If the Local engine cannot produce a renderable diagram in under two minutes on an M-series laptop, say so and give the fallback (HTML/SVG written by the run instead of drawio, or a pre-baked artifact).
4. **Demo action items.** Give the exact two spoken sentences that (a) the detector at `src-tauri/src/copilot_session.rs::detect_commitment` catches and (b) map to Research and Visualize once `commitment_capability` gains a visualize branch (a worker is adding it now; assume words like diagram, architecture, draw, chart map to visualize). Give the task title and details each produces and what artifact name to expect.
5. **Cut list.** Ordered. What to drop first if the frontend worker runs late.

Format: numbered sections matching the above, code blocks for JSX/CSS, file:line anchors for every claim about existing code.
