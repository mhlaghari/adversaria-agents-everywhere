# Acceptance — 0.3.76 (macOS, beta)

_The fresh-machine pass the remediation plan requires before any release is
published (plan §1.1 / marketable-bar). Fill every box during the run; a box
you can't tick honestly is a finding, not a formality. Runner: Hamza.
Machine: the 8 GB MacBook (or a fresh user account on this Mac if the MacBook
isn't at hand — note which)._

**Build under test:** `Adversaria-0.3.76-beta-macos-arm64.dmg` (local, stapled —
this pass gates the publish, so the download-from-website step is replaced by
opening the local DMG; the website link gets verified post-publish by
`verify-published.sh`).

## Setup
- [ ] Fresh macOS user account created (`sysadminctl -addUser adversaria-qa -password <pw> -admin` or System Settings), logged in — no prior TCC grants, cold caches
- [ ] Machine/account noted: ______________

## The journey
- [ ] DMG opens; Gatekeeper shows the notarized-app flow (no "unidentified developer" wall)
- [ ] Drag-install to /Applications; first launch completes (note the time-to-first-window: ______)
- [ ] Setup wizard: mic + screen-recording prompts REALLY appear (this account has no stale grants) and granting works; restart asymmetry copy shown for screen recording
- [ ] Model download: starts on click (never automatically), progress bar moves continuously, no stall at 0% — duration: ______
- [ ] **Real meeting, ≥10 min, two voices** (a YouTube interview + your own voice works): record, stop, transcribe
- [ ] Transcript sane: both Me/Them channels present, no hallucinated tails, vocabulary correct
- [ ] Notes generated with the DEFAULT local model: structured note, no raw JSON, to-dos extracted with owners
- [ ] Recording deleted after successful transcription (check the spool dir is gone)
- [ ] Relaunch: history intact, no recovery prompts for the completed meeting
- [ ] Settings → Export diagnostics: bundle generates; open it and confirm NO transcript text/titles/keys
- [ ] Quit: no orphaned `adversaria-service` in Activity Monitor

## 0.3.76-specific checks
- [ ] Kill the sidecar mid-session (Activity Monitor → adversaria-service → Force Quit ×4): the restart-exhausted error shows a REAL reason/traceback tail, not just "Local AI Offline"
- [ ] Notes with a verbose model (if pulled on this account — optional): no raw-JSON note, honest error copy if anything fails

## Verdict
- [x] **PASS — publish authorized by the founder, 2026-08-11** ("Okay, pass
  and publish"). Recorded honestly: the checklist boxes above were NOT
  individually executed for this release — the founder waived the full
  fresh-account run. The machine gates that DID run and pass: frozen-sidecar
  smoke (boot → transcriber ready → real /transcribe), notarization
  Accepted, staple validate, provenance clean, stable-name copy
  byte-identical, post-publish verifier (see publish record in HANDOFF).
  The full fresh-account journey remains OWED before any traffic-flood
  marketing (it is a marketable-bar item, not a per-beta-release nicety) —
  carry it forward to the launch-gating release.
- Findings / notes: publish was macOS-only (deliberate, Windows amendment).
- Date + duration of pass: waived 2026-08-11; machine gates only.
