# Phase 0 visual baseline

These screenshots are captured from the hermetic debug build with
`npm run test:e2e:baseline`. They cover the supported minimum window size and a
representative wide desktop size. The fixture contains no user meeting data.
