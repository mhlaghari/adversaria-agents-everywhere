# SPEC

_The product contract — a **living doc**. What this is and where it's going
(`CLAUDE.md` covers the current code; this covers the destination). Every session
reads this first; keep it current. Strategy rationale: [docs/STRATEGY.md](./docs/STRATEGY.md)._

## Vision
**Adversaria** is a privacy-first, bot-free meeting notetaker for Windows and macOS (Apple Silicon) that records meeting audio locally, transcribes it on-device, and generates structured notes with a local LLM — **nothing leaves the machine**. No bot joins the call; no audio, transcript, or summary is uploaded; recordings are deleted once a meeting is successfully transcribed (a recording that couldn't be transcribed — e.g. the AI service was down — is kept on-device, clearly marked, until you retry). The destination is not "another Granola clone" but the **sovereign capture organ of lagharilabs OS**: the complete capture → memory → action loop that runs entirely on the customer's own infrastructure, for buyers (legal, healthcare, EU/sovereign, defense) who are legally or strategically barred from cloud AI.

## Principles (load-bearing — do not violate)
1. **Privacy is the product.** Default to 100% local/offline. Audio is deleted after transcription. Never add a code path that sends audio, transcripts, or summaries off-device unless the user *explicitly* opts in for that specific run and the UI says so. The webview never gets network access to the Python service — Rust talks to it via `reqwest`.
2. **Research the latest first.** Never rely on training memory for anything versioned (Tauri 2, faster-whisper/CTranslate2, uv, Ollama, MLX, Pydantic, httpx). Verify current docs/versions; when memory and a fetched source disagree, the source wins.
3. **Verify against reality.** Read the file before editing, run the tests, run the app, check the DB/config on disk. Report behavior plainly — including failures.
4. **Many small, focused files; immutability; explicit errors** at every boundary, with user-facing messages.
5. **Leave the docs better than you found them.** Update the relevant companion doc in the same change.
6. **It must work without the user touching anything.** (Founder, 2026-08-11, verbatim: _"I'm not going to do anything in the back settings. Neither are the people who will use this. If it's Qwen, Gemma, or Muse, it should work for all."_) When a value depends on the machine, the model, or the input — context window, thread count, timeouts, model choice — **compute it at runtime** from what can be measured (RAM, the prompt, the model's own `/api/show` metadata). An env var or Settings toggle is acceptable only as a **debugging override that is never required**, and per-model hardcoded tables are a bug: the next model the user installs isn't in them. A knob the user must find to get a correct note is a defect, not a feature.

## Scope
- **In:** on-device record → transcribe → summarize → store → review; dual capture (system + mic); speaker labels incl. **anonymous Speaker-N diarization of the system channel** (sherpa-onnx, on by default); multilingual/RTL summaries; meeting auto-detect; chat-with-meeting; verbatim user notes; personalization (name, vocabulary); user-editable prompt templates; Windows (WASAPI/CUDA) + macOS (ScreenCaptureKit+cpal/MLX). **(2026-07-02, all shipped in v0.3.14)** audio-file import (voice memo → notes); dark **slide export** (self-contained HTML → browser one-page PDF) + Markdown export; **meeting bundle export/import** (`*.adversaria.json`, federated fresh-id import) + backup-all/restore (Settings → Data); in-app **Knowledge Graph tab** (meetings/people/tags/owners from SQLite, zero LLM, cytoscape + infinite d3-force). **(2026-06-20)** an **opt-in cloud-LLM provider** picker (Grok / OpenRouter / any OpenAI-compatible) for users without a local GPU — **off by default, clearly labeled that the transcript leaves the device** (Principle 1); attendee-name normalization (dedup + roster grounding); calendar integration for attendee rosters — **macOS via EventKit** (zero sign-in: reads the calendars already in the Mac's Calendar app via one OS permission, no OAuth/backend) and **Google Calendar** read-only OAuth (tokens in OS keychain) as the Windows/cross-platform path — **off by default** either way. **(2026-07-10, shipped in v0.3.32)** **category→template auto-routing** (default-template summaries detect what the recording is — hint > one-word local-LLM classify > heuristic — and pick the matching template; an explicit manual pick is never overridden) + a **bidirectional interview template** (detects whether the user is interviewer or candidate); **sidebar auto-archive** (Pinned + last-N-days resting view, older meetings fold into a searchable Archive; `archive_after_days` setting), selected-meeting highlight, clear-ALL-filters, and **@person attendee search chips**. **(2026-08-05, shipped in v0.3.73)** **OS notifications, every one of them user-controllable**: the pre-meeting calendar alert (`meeting_reminder_enabled`, off by default) and the daily due/overdue **to-do digest** (`todo_digest_enabled` + `todo_digest_hour`, on by default because it shipped ungated and silently switching it off would remove a notification users already receive). The digest previously had **no setting at all** — the product must never interrupt the user in a way they cannot switch off.
- **In, working tree (2026-08-30; not yet released):** a project is the meeting-facing form of a long-lived workspace. Users can create colored project folders, file one meeting into one project, view a locally generated overview and attendee frequency across the filed meetings, manage the project's open action items, and set standing instructions. The overview is grounded only in local filed-meeting content and never browses the web. Deleting a project deletes the container and its bindings, never the meetings.
- **Out (for now):** off-device processing *as the default* (the cloud-LLM path above is strictly opt-in and labeled; local stays the default and the product's point); **Microsoft** calendar (Google read-only OAuth is built; MS is the next provider — `docs/SPEC_CALENDAR.md`); **named**-speaker identification (anonymous Speaker-N diarization IS built — mapping those labels to real identities is specced in `docs/SPEC_DIARIZATION.md`, not built); pre-meeting notes (undecided); a *signed/notarized, distributable* installer (a personal unsigned macOS `.dmg` is built — `./scripts/build-dmg.sh` — but code-signing + notarization for distribution is a hard **launch gate**, not yet done); **in-binary Pro/license gating at launch** (decided 2026-06-28: **launch free-only**, build the Merchant-of-Record + offline license path and switch Pro on ~30–60 days later — capture Pro intent via a waitlist meanwhile; the paid wedge targets regulated client-facing solos, not broad prosumers — see [STRATEGY.md](./docs/STRATEGY.md) §2026-06-28 + [docs/LAUNCH_PLAN.md](./docs/LAUNCH_PLAN.md)).

## Open decisions
- **Pre-meeting notes (calendar prep)** — read the calendar and prompt the user with prep before a meeting. _Options:_ suggested questions / agenda / attendee+topic history; or don't build. _Lean:_ undecided — clarify the exact value before committing (pairs with cross-meeting RAG + ADR-002 read-only OAuth, off by default).
- **Next project-memory slice** — surface related meetings beneath an individual note using the retrieval already present in `workspace_runs.rs`; then continue the wider workspace-screen rebuild. Project filing and ProjectView are complete in the working tree, pending founder acceptance and commit authorization.
- **LLM backend** — local default (Rapid-MLX on macOS / Ollama elsewhere) vs. an OpenAI-compatible endpoint. _Resolved 2026-06-20:_ a Settings **Provider** picker ships — Local (default) / Grok / OpenRouter / OpenAI-compatible custom; cloud is opt-in + labeled. Backend is per-request (base_url/api_key from config), no restart.

## Changelog
- 2026-08-30 — Added Meeting projects to in-scope product behavior: one workspace-backed project per meeting, local source-grounded overview, standing instructions, and deletion that preserves meetings. Replaced the obsolete pre-board build-target decision with the next project-memory slice. Implementation remains uncommitted.
- 2026-08-11 — **Principle 6 added ("it must work without the user touching anything"), from the founder verbatim.** Not a version entry — a rule recorded so it isn't undone. Its first application: the summarizer's LLM context window (`num_ctx`) now sizes itself per request from the prompt, the machine's RAM, and the model's own reported maximum, replacing a fixed 16,384 that truncated a long meeting under a verbose reasoning model (the 08-10 Muse Glimmer incident). No setting was added and none should be; the two env vars are debugging-only overrides. Live-proven + measured — detail in [HANDOFF.md](./HANDOFF.md), gotchas in [docs/LESSONS_LEARNED.md](./docs/LESSONS_LEARNED.md).
- 2026-07-24 — **v0.3.58**: **on-device model choice in the user's hands** — setup now leads with one clear RAM-sized recommendation (the alternative one click behind "Change model"), and **Settings → AI Engine** gains a model picker to switch the pinned local profile after setup (marks the recommended + active one, per-row Use/Download/Retry, takes effect without an app restart). Recommend, never force. Also fixed a latent **auto-update** bug: installed beta builds poll `latest-beta.json` but the publish script wrote `latest.json`, so the "Update available" prompt could never fire — the publisher now writes the channel-matched manifest. (Detecting/reusing models already pulled via Ollama is deferred — a cross-runtime follow-up.)
- 2026-07-21 — **v0.3.57 shipped**: a **floating recording pill** — switching away from the app mid-recording drops a compact pill in below the notch (dot · timer · live waveform), with an **Expressive** island option (timer, the live caption, both channels, one-tap Stop) or **Hidden**, chosen in Settings → Notch & alerts. Plus two fixes surfaced during the build: recovery of pending recordings moved off the launch critical path (it was hanging launch behind a hidden macOS Keychain prompt after a re-signed build), and the recording-spool encryption key is now cached (≤ 1 Keychain prompt per launch; recordings stay encrypted at rest). A macOS NSPanel "float over fullscreen calls" was attempted and **reverted** (it crashed during recording); deferred to a prototyped redo ([docs/NOTCH_PILL_SCOPE.md](./docs/NOTCH_PILL_SCOPE.md)). (0.3.39–0.3.56 were internal iterations toward this.)
- 2026-07-11 — **v0.3.38 shipped**: presenter-misattribution fix made structural — youtube-template summaries neutralize viewer-labeled lines ("Viewer mic (not the presenter)") before the LLM sees them, after two prompt-level fixes failed live tests on a real mic-bleed transcript (49/99 lines under the viewer's name). Doctrine recorded: accuracy is won at the stage the error enters; prompts are advisory, structure is enforceable. Accuracy deep-dive + ranked plan logged in docs/TODO.md.
- 2026-07-10 (final) — **v0.3.37 shipped**: pixel polish from the v0.3.36 smoke-test — sidebar rows lose the dead space after the date (the hover-only ⋯ button no longer reserves layout; it overlays and swaps with the date), Insights column centered.
- 2026-07-10 (latest, night) — **v0.3.36 shipped**: **Meeting Insights** — on-device speaking stats (talk-time share, WPM vs 130–175, filler rate vs 4%, interruptions, longest monologue; zero LLM — read.ai's speech-based coaching half without the camera-based affective layer, which we reject by thesis) + **timestamped transcripts** (turn start/end preserved end-to-end; `[MM:SS]` prefixes) + **`detailed` template** + injection-guard rule in all templates + youtube viewer≠presenter fix. Meetily round-2 verdict in TODO.md.
- 2026-07-10 (night) — **v0.3.35 shipped**: settings-application fix — `sidebar_view` and `archive_after_days` now apply when the user saves and returns to Meetings (previously silently restart-only; the user hit it).
- 2026-07-10 (latest) — **v0.3.34 shipped**: sidebar style is now a Settings choice — Compact rows (default) vs the classic Full cards, sharing the same bins/archive/@person behavior — and @person chips render inside the search bar (bar chrome moved from the input to a `.search-bar` wrapper with a `:focus-within` ring).
- 2026-07-10 (later) — **v0.3.33 shipped**: sidebar v2 after the user's live smoke-test of v0.3.32 — **manual archive** (new `archived` column; ⋯-menu Archive/Unarchive; archiving unpins), **compact one-line rows** with hover peek (the "B" design), **real date bins** (Today/Yesterday/This week/month bins replacing the single last-N-days section; the age rule still feeds Archive), scrollable @person dropdown. Same stunt-delegation process.
- 2026-08-05 — **v0.3.73 shipped (beta channel)**; scope updated. Two threads: (1) four verified defects closed, three of which could kill the Windows sidecar on every launch — a macOS-only data path resolved in the frozen build on every platform, a parent-death guard that hard-exited on any unreadable stdin, and a native import at module scope that died before uvicorn could bind; (2) **every OS notification is now user-controllable**. The daily to-do digest had fired twice a day since it was built with no setting and no way to stop it; it is now gated (default on, configurable hour) — recorded here because "cannot be turned off" is a scope/principle question, not just a bug. Also on `master`: a Settings redesign **proposal only** (readiness ledger, destination-first, visual previews for the notch pill), blocked on per-field config patching. Detail in [HANDOFF.md](./HANDOFF.md) + [docs/TODO.md](./docs/TODO.md).
- 2026-07-10 — **v0.3.32 shipped to the installed app** (committed + signed-frozen + verified): **category→template auto-routing** with a new **bidirectional interview template** (Python `auto_template` flag; manual picks never overridden; fail-open), and the **sidebar declutter bundle** — auto-archive zones (`archive_after_days`), selected-meeting highlight, clear-all-filters, `@person` attendee search chips. Scope "In" updated accordingly. Built via stunt-worker delegation (Claude: spec/review/verify only).
- 2026-07-02 — **v0.3.14 shipped to the installed app** (committed + re-frozen + verified): meeting **export/import bundles + backup-all**, the in-app **Knowledge Graph tab** (decluttered data model + Obsidian-style infinite force simulation), recording-bubble elapsed timer, Weekly/To-dos declutter, prompt grounding parity across all templates, and two diarization-rooted fixes (phantom-speaker merge; watched videos no longer classified "brainstorm"). Scope "In" updated to record audio import + slide export (shipped 2026-06-30/07-01) and the above; "Out" corrected — anonymous diarization is built, only *named*-speaker identification remains out.
- 2026-06-28 — **Launch decision codified** (from [docs/LAUNCH_PLAN.md](./docs/LAUNCH_PLAN.md), founder-approved): launch **free-only** (Pro deferred ~30–60 days, no in-binary gating at launch — waitlist instead), paid wedge = **regulated client-facing solos** (prosumers + Arabic/RTL = free funnel), lead with "nothing leaves your machine", **open-source the read-only MCP server**. Four hard launch gates now tracked in [docs/TODO.md](./docs/TODO.md). Strategy reconciliation: [docs/STRATEGY.md](./docs/STRATEGY.md) §2026-06-28. Privacy Principle 1 unchanged.
- 2026-06-20 (pm) — Scope updated for the relay build (branch `feat/settings-providers-calendar`, not yet merged): opt-in cloud-LLM provider picker (local stays default + labeled), attendee-name normalization, and **Google** Calendar read-only OAuth (off by default, keychain tokens). Microsoft calendar is the next provider. Privacy Principle 1 unchanged — local-by-default, cloud strictly opt-in.
- 2026-06-20 — Scope updated: personal macOS `.dmg` now builds (`./scripts/build-dmg.sh`); signed/notarized distribution still out. Diarization + calendar now have specs in `docs/`.
- 2026-06-19 — created during stuntman scaffold; populated from CLAUDE.md, docs/HANDOFF.md, docs/STRATEGY.md.
